operadores 
operadores aritméticos 
suma
símbolo +
select * from notas_alumnos;

insert into notas_alumnos
values(3,'cesar','matematicas',16,13,10,0);

select codigo_al, nombre_al, nota1 + nota2 + nota3 as suma_nota
from notas_alumnos;

select codigo_al, nombre_al , (nota1+nota2) as Acumulado_nota1_nota2, nota3
from notas_alumnos;

select codigo_al, 
nombre_al, 
(nota1+nota2) as acumulado_nota1_nota2,
nota3, 
(nota1+nota2)-nota3 as acumulado_nota1_nota2_menos_nota3
from notas_alumnos;

multiplicación
*
división
/
redondeo
round()


operadores relacionales o de comparación 

operadores de concatenación

operadores lógicos
